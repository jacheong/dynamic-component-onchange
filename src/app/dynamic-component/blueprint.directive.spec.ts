import { BlueprintDirective } from './blueprint.directive';

describe('BlueprintDirective', () => {
  it('should create an instance', () => {
    const directive = new BlueprintDirective();
    expect(directive).toBeTruthy();
  });
});
